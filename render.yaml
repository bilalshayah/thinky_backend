services:
  - type: web
    name: thinky-backend
    runtime: python
    rootDir: thinky
    buildCommand: pip install -r requirements.txt
    startCommand: python manage.py migrate --noinput && gunicorn thinky.wsgi --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.7
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        generateValue: true
